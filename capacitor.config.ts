import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.current.moby',
  appName: 'moby-frontend',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
